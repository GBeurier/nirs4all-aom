# AOM paper cohort manifest

Auto-generated by `paper_aom/review/build_cohort_manifest.py`.
Do not edit by hand. Re-run the script to refresh.

## Summary counts

- Regression cohort: **61 included / 61 total** (all rows with `status==ok` in `cohort_regression.csv`).
- Classification cohort: **16 included / 17 total** (`status==ok` in `cohort_classification.csv`).
- QUARTZ datasets present: **1** (kept in absolute-error tables; excluded from pairwise ratios -- see rule below).
- TabPFN-allowed regression datasets: **16** (n_features <= 500 AND n_train <= 10000).
- TabPFN-allowed classification datasets: **2**.
- AOM-Ridge-global-allowed regression datasets: **61** (n_features * n_train < 100,000,000).

## Denominators per analysis

| analysis | included_n | denominator_rule | primary_metric |
| --- | --- | --- | --- |
| full regression cohort | 61 | status==ok in cohort_regression.csv | RMSEP (absolute) |
| paired AOM-PLS vs PLS | 61 | has_pls AND has_aom_pls | RMSEP / RMSEP_PLS |
| paired AOM-Ridge vs Ridge | 57 | has_ridge AND has_aom_ridge | RMSEP / RMSEP_Ridge |
| paired AOM-PLS vs TabPFN-HPO | 58 | has_tabpfn_hpo AND has_aom_pls | RMSEP / RMSEP_TabPFN-opt |
| TabPFN-allowed regression subset | 16 | n_features<=500 AND n_train<=10000 | balanced ratio or absolute RMSEP |
| AOM-Ridge-global allowed subset | 61 | n_features*n_train<100,000,000 | RMSEP (single-machine path) |
| full classification cohort | 16 | status==ok in cohort_classification.csv | balanced accuracy, macro-F1, log-loss, ECE |
| TabPFN-allowed classification subset | 2 | n_features<=500 AND n_train<=10000 | balanced accuracy |

## Per-domain breakdown (regression, included)

| domain_group | n_datasets | mean_p_over_n_train |
| --- | --- | --- |
| leaf-physiology | 21 | 6.74 |
| fruit-quality | 5 | 8.93 |
| soil-amendment | 5 | 2.92 |
| crop-grain | 3 | 9.19 |
| crop-seed | 3 | 0.95 |
| dairy | 3 | 0.67 |
| petroleum | 3 | 3.19 |
| plant-disease | 3 | 4.78 |
| soil-eu | 3 | 1.53 |
| beverage | 2 | 14.40 |
| biomedical | 2 | 12.53 |
| food-product | 2 | 17.50 |
| wood-product | 2 | 4.81 |
| industrial | 1 | 5.91 |
| meat-quality | 1 | 0.60 |
| mineral | 1 | 7.08 |
| pharmaceutical | 1 | 1.95 |

## QUARTZ treatment rule

The `QUARTZ_spxy70` dataset has reference RMSE near 3.4e-9 (essentially zero). Pairwise ratios such as RMSEP/RMSEP_PLS therefore blow up by ~6 orders of magnitude for any non-trivial candidate. Rule:

- **keep** QUARTZ rows in absolute RMSEP / failure tables;
- **exclude** QUARTZ rows from pairwise ratio summaries (median, IQR, win-counts);
- the manifest column `exclusion_reason` is set to `denominator_near_zero_pairwise` for QUARTZ; `status_in_primary_analysis` stays `include`.

**Post-session clarification (2026-05-13)**: `Xtrain.csv` for `QUARTZ_spxy70`
is no longer present on disk (`bench/tabpfn_paper/data/regression/QUARTZ/`).
This forced a second-level exclusion of QUARTZ from the new multi-seed runs
launched in this session (`status` was reset to `missing_data` in
`bench/AOM_v0/benchmarks/cohort_regression.csv`). The absolute-error rows
for QUARTZ in the failure tables therefore come from the single-seed
reference run `bench/AOM_v0/benchmark_runs/full/results.csv` (134 Quartz
rows). The manifest row still reads
`status_in_primary_analysis=include, exclusion_reason=denominator_near_zero_pairwise`
because the rule is unchanged; if the missing file is restored, a rerun on
the new cohort would extend the absolute-error coverage. For paired tables
the canonical denominators (53 paired vs PLS, etc.) already reflect the
QUARTZ exclusion.

## Inclusion / exclusion rules

1. `status_in_primary_analysis = include` iff the source cohort row has `status==ok`; the manifest preserves the original `reason` in `exclusion_reason` otherwise.
2. The full regression cohort denominator is 61 (all `ok` rows). The historical 57-dataset headline is derived by also requiring `has_pls AND has_aom_pls` -- this lives in the paired tables above.
3. Pairwise tables vs Ridge / TabPFN use the corresponding has_* intersection; each caption must cite the denominator from this manifest.
4. `tabpfn_allowed = (n_features <= 500) AND (n_train <= 10000)`. Source: TabPFN paper Sec. 3.4 (sample-then-truncate path; PFN backbone trained on <=500 features; v1 inference cost is quadratic).
5. `aomridge_global_allowed = (n_features * n_train < 1e8)`. The AOMRidge-global path materialises full Gram blocks; with 62 GB RAM on the benchmark host (Linux 6.6, WSL2, 64 GB total), 1e8 keeps peak RSS under ~30 GB. Datasets above the threshold must be routed through Local/MKL variants.
6. Classification rows for which a result CSV is missing (e.g. `COFFEE_sp/Species_56_Bagnall`) carry `exclusion_reason` from the cohort CSV (e.g. `missing files: ...`).

## Source CSVs scanned for `has_*` flags

- `bench/scenarios/runs/exhaustive_research_full57_seed0/results.csv` (found)
- `bench/scenarios/runs/best_current_full57_seed0/results.csv` (found)
- `bench/AOM_v0/benchmark_runs/full/results.csv` (found)
- `bench/AOM_v0/Ridge/benchmark_runs/classification_all17/results.csv` (found)
- `bench/master_results_classif.csv` (found)
- `bench/1_master_results.csv` (found)

